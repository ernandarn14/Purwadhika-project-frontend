import Axios from "axios";
import Cookie from 'universal-cookie';
import { API_URL } from "../../constants/API"

const { WISHLIST_SUCCESS, WISHLIST_FAILED, LIKE_COUNT } = types;

export const addWishlist = () => {
    return (dispatch) => {
        
    }
}